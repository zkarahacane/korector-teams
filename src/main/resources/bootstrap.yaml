spring:
  application:
    name: teams
  cloud:
    config:
      discovery:
        enabled: true
        service-id: config
      failFast: true
eureka:
  client:
    serviceUrl:
      defaultZone: ${EUREKA_URI:http://localhost:8761/eureka}
  instance:
    preferIpAddress: true
